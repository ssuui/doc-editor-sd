# 本地启动

## 基础依赖

- Node.js 20.x
- pnpm 9.x
- Go 1.26+
- Docker Desktop

## 启动步骤

1. 执行 `pnpm install` 安装前端依赖。
2. 执行 `go mod tidy` 拉取后端依赖。
3. 使用 `docker compose up -d` 启动数据库与对象存储。
4. 分别运行前后端服务，确认首页和健康检查可访问。

## 自检清单

- 登录页可以正常进入。
- 文件树能加载 `source_root`。
- Markdown 文件修改后能看到未保存提示。
