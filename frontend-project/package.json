{
  "name": "frontend-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p dist/assets && ./node_modules/.bin/esbuild src/main.tsx --bundle --minify --sourcemap --outfile=dist/assets/app.js --loader:.ts=ts --loader:.tsx=tsx --loader:.css=css && cp public/index.html dist/index.html && cp public/login.html dist/login.html && cp src/styles.css dist/styles.css"
  },
  "dependencies": {
    "marked": "^4.3.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.5.4"
  }
}
